Compose File (Docker or Podman) for https://github.com/sigoden/dufs

How to use:
- clone this repo
- `cd dufs-compose`
- `docker-compose up -d`

Notes:
- see `config.yml` to configure sigoden/dufs
- `assets` folder from the sigoden/dufs
- to enable HTTPS see `tls` folder